export const enum ActionState {
    PRE_LOAD = 'PRE_LOAD',
    AFTER_LOAD = 'AFTER_LOAD',
    CREATE = 'CREATE',
    READ = 'READ',
    UPDATE = 'UPDATE',
    DELETE = 'DELETE',
    REFRESH = 'REFRESH',
    ENQUEUE = 'ENQUEUE',
    DEQUEUE = 'DEQUEUE'
}
